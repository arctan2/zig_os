pub const Task = struct {
    registers: struct {
        r0: usize,
        r1: usize,
        r2: usize,
        r3: usize,
        r4: usize,
        r5: usize,
        r6: usize,
        r7: usize,
        r8: usize,
        r9: usize,
        r10: usize,
        r11: usize,
        r12: usize,
        r13: usize,
        r14: usize,
    },
};
